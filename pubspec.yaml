name: diary_cipher
description: 跨平台日记加密应用
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  crypto: ^3.0.3
  path_provider: ^2.1.0
  intl: ^0.18.0
  file_picker: ^6.1.1
  universal_html: ^2.2.4
  pointycastle: ^3.9.1           # ✅ 关键依赖

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true